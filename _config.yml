title: Arkansans for Pension Integrity
description: "Arkansas pension funds authorized up to $100 million in non-tradable foreign sovereign debt without independent analysis. We're pushing for pension transparency legislation in 2027."
url: "https://arpensions.org"
baseurl: ""
permalink: /:title/
locale: en_US

# Social / Open Graph defaults
twitter:
  card: summary_large_image
social:
  name: Arkansans for Pension Integrity

# Build settings
markdown: kramdown
plugins:
  - jekyll-seo-tag
  - jekyll-sitemap

# Collections
collections:
  pages:
    output: true
    permalink: /:title/

# Defaults
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "page"
  - scope:
      path: ""
    values:
      image: /assets/images/og-default.png

# Exclude from build
exclude:
  - README.md
  - CLAUDE.md
  - LICENSE
  - CNAME
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor
