title: Arkansans for Pension Integrity
description: "Public records document Arkansas pension and Treasury decisions involving Israel Bonds, a form of non-tradable sovereign debt. Arkansans for Pension Integrity advances issuer-neutral safeguards for public pension decisions."
url: "https://arpensions.org"
baseurl: ""
permalink: /:title/
locale: en_US

# Social / Open Graph defaults
twitter:
  card: summary_large_image
social:
  name: Arkansans for Pension Integrity

# Build settings
markdown: kramdown
plugins:
  - jekyll-seo-tag
  - jekyll-sitemap

# Collections
collections:
  pages:
    output: true
    permalink: /:title/

# Defaults
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "page"
  - scope:
      path: ""
    values:
      image: /assets/images/og-default.png

# Exclude from build
exclude:
  - _internal
  - README.md
  - CLAUDE.md
  - LICENSE
  - CNAME
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor
  - scripts
  - requirements-dev.txt
  - .github
